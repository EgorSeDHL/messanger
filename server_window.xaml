<Window x:Class="_6_практос.server_window"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:_6_практос"
        mc:Ignorable="d"
        Title="server_window" Height="450" Width="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="117*"/>
            <ColumnDefinition Width="43*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="192*"/>
            <RowDefinition Height="25*"/>
        </Grid.RowDefinitions>
        <ListBox x:Name="list_box" d:ItemsSource="{d:SampleData ItemCount=5}" Height="NaN" Margin="0,0,0,10" Grid.ColumnSpan="2" SelectionChanged="list_box_SelectionChanged"/>
        <TextBox x:Name="text_box" HorizontalAlignment="Stretch" Margin="0" Grid.Row="1" TextWrapping="Wrap"  VerticalAlignment="Stretch" />
        <Button x:Name="send_nudes" Grid.Column="1" Content="Отправить" HorizontalAlignment="Stretch" Margin="10" Grid.Row="1" VerticalAlignment="Stretch" Click="send_nudes_Click" />

    </Grid>
</Window>
